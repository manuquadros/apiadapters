# Changelog

Every released version of apiadapters, newest first, generated by
`pdm run changelog` from the commit history. Do not edit by hand.

## Unreleased

### Fixed

- Point xmlparser to the git repo in apiadapters/pyproject.toml (`46a21fa`)
- Eliminate TypeError from exception raising (`8dab2ef`)
- Call super().__aexit__ to prevent dangling HTTP connection pools (`1ae193d`)
- Remove mutable defaults from callers (`b05b358`)
- Make only 429 retryable (`d2b5525`)
- Let xpath(...)[0] IndexError propagate (`f24ecca`)
- Use etree.tostring to avoid missing abstracts starting with tags (`88cc895`)
- Include pubmed_id in the async fetch_fulltext_and_abstract (`3de7889`)
- Serialize only the inner content from <AbstractText> (`a99c646`)
- Get string from request url before passing to the response_handler (`4c7283a`)
- Call super().__init__() from the base class to keep cooperative multiple-inheritance flowing (`0ead8b8`)
- Port Culture/StringSet locally, import Annotated from typing (`1a223f4`)
- Retry only 429 in retry_if_too_many_requests (`0115236`)
- Import Strain from local straininfo module, not d3types (`e86b799`)

### Changed

- Remove d3types dependency (`cde336b`)
- Remove unnecessary closure (`2601719`)
- Use module-level logger for diagnostic output (`3fea13a`)
- Remove singledispatchmethod from static method (`01c3fee`)
- Make namespaces global to the module (`ff6763c`)
- Keep self.api_key always set (`fa5f3ec`)
- Use Element.findtext to avoid AttributeError (`44951cb`)
- Make retmax explicit in the esearch call (`2b9051f`)
- Make the task structure explicit in the typing (`91fa1f8`)
- Eliminate all storage concerns from straininfo.py (keep it as a stateless HTTP client) (`7e7cb6e`)
- Remove evil mutation, return new dict from retrieve_strain_models, make it composable (`27bb733`)
- Extract common logic between the sync/async versions (`6834812`)
- Use ncbi's webenv to query multiple ids (`2c0ba88`)
- Replace hand-rolled retry with tenacity (`ac3d16b`)
- Replace the adapters' TinyDB storage with a sink callback (`a2d3cc9`)

### Internal

- Migrate to pdm (`6deb106`)
- Fix pandas import (`cd97712`)
- Remove redundant definition (`a1fb3a7`)
- Remove shadowed import (`408dc23`)
- Remove unused import (`566e47a`)
- Add pytest-cov to dev dependencies (`a7fe38e`)
- Add a battery of tests (`8da8602`)
- Add straininfo integration tests (`6573a43`)
- Add integration tests for ncbi (`b2617a5`)
- Fix urls in pyproject.toml and add requirements.txt (`fc4aa6d`)
- Fix extract_body docstring (`bbc0534`)
- Add missing dev deps (`9ae6d14`)
- Add pdm.lock, pylock.toml, uv.lock (`01451a7`)
- Verify lock files stay in sync with pyproject.toml (`656cf67`)
- Declare tinydb and pydantic as dependencies (`aaa80b8`)
- Declare pytest dev dependency group (`72ce85f`)
- Add scripts/lock.sh to regenerate the three lock files (`fc4e502`)
- Run unit tests on push and pull request (`add683b`)
- Pin pdm and uv versions in lockfiles workflow (`c3f30ae`)
- Add README with install and usage instructions (`038ee43`)
- Regenerate requirements.txt and add drift check to CI (`96a4341`)
- Drop tinydb dependency and relock (`2ceba1b`)

## 0.1.0 — 2025-10-06

### Added

- Add functions to extract abstract and fulltext from PMC entry (`8b8f3e6`)
- Add StrainInfo API adapter implementation (`1c7faca`)
- Provide synchronous version of APIAdapter and NCBIAdapter (`85b5a2d`)
- Provide async and synchronous version of StrainInfoAdapter (`56ea168`)
- Extract_pmid (`952f36e`)

### Fixed

- Full_text > fulltext (`2403f6c`)
- Increase httpx client timeout to 100 seconds (`51941e5`)
- Initialize the base adapters properly (`452757d`)

### Changed

- Update NCBIAdapter to inherit from AsyncAPIAdapter (`aadb665`)
- Replace APIAdapter with AsyncAPIAdapter in import (`d813dda`)
- Update import path for brenda_types in straininfo adapter (`9339158`)
- Replace ABC with Protocol in StrainInfoAdapterBase and add initialization print (`b051c19`)
- Remove unused import from straininfo.py (`df5352a`)
- Update StrainInfo adapter initialization and method visibility (`d1a0bcf`)
- Change private method visibility from double to single underscore (`7245844`)
- Incorporate lpsn_interface (`ee2c604`)
- Remove lpsn_interface (`ee1f168`)
- Internalize and remove loggers dependency (`b0a15c8`)

### Internal

- Log -> loggers (`b44d44e`)
- Export normalize_strain_names (`6878bbe`)
- Add d3types dependency (`b868985`)
- Add d3types as submodule (`d73f252`)
- Remove submodules (`0f14090`)
- Resolve d3types from git (`622b0a9`)
- Add package description (`f0cf7e6`)
- Add license (`4cb85af`)
- Pin xmlparser and d3types versions (`436431d`)

