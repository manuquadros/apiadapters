[project]
name = "apiadapters"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11, <4"
dependencies = [
    "d3tools",
    "httpx>=0.28.1",
    "lxml>=5.3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
d3tools = { workspace = true }
